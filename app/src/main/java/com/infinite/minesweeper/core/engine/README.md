# T8: game engine

Put the `GameEngine` implementation, reveal/flag/chord rules, bounded breadth-first flood-fill,
batched state publication, auto-flagging, and chunk-cleared event emission here.

The T9 lock mechanic belongs in the `lock` child package.
