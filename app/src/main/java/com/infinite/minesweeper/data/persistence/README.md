# T10: persistence wiring

Put dirty tracking, engine/repository coordination, metadata persistence, viewport-only restore,
and lifecycle flush behavior here. Room schema details remain in `data/db`.
